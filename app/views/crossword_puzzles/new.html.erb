<% if current_user %>

	<% if current_user.admin %>

		<h1>New crossword_puzzle</h1>
		
		<%= render 'form' %>
		
		<%= link_to 'Back', crossword_puzzles_path %>
		
	<% else %>
		
		
		
	<% end %>
	
<% else %>
	You must make an account and log in to begin creating your own puzzles
<% end %>

