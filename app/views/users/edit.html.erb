<% if current_user && current_user.admin %> 

	<h1>Editing user</h1>
	
	<%= render 'form' %>
	
	<%= link_to 'Show', @user %> |
	<%= link_to 'Back', users_path %>

<% else %>

	<%= render :partial => "partial/admin_only" %>

<% end %>